#pragma once

#include "RenderableHelper.h"
#include "renderables/Mesh.h"

namespace SceneImporterInternal {
    class MeshHelper : public RenderableHelper {
        // Shared logic specific to Mesh if any, otherwise just a categorization layer
    };
}
